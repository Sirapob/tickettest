version: "2"
options:
  verbose: true
  unison_image: "eugenmayer/unison:latest"
syncs:
  apicode-unison-sync:
    src: "./api"
    sync_host_ip: "auto"
    sync_host_port: 10872
    sync_strategy: "unison"
  uicode-unison-sync:
    src: "./ui"
    sync_host_ip: "auto"
    sync_host_port: 10873
    sync_strategy: "unison"

